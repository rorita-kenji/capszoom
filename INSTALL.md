# CapsZoom インストール手順

Apple Silicon の Mac 向けです。Intel Mac では動きません。

---

## 1. アプリを入れる

1. 配布された `CapsZoom.dmg` をダブルクリックして開く。  
   （GitHub から取る場合: https://github.com/rorita-kenji/capszoom/releases の最新の `CapsZoom.dmg`）
2. 開いたウインドウに `CapsZoom` と `Applications` がある。
3. **CapsZoom を Applications へドラッグ**する。
4. DMG のウインドウは閉じてよい。デスクトップに残ったディスクアイコンは右クリック → 取り出す。

---

## 2. 初回起動（「開発元を確認できない」）

有料の Apple 開発者IDで署名していないため、ダブルクリックだけだと止まることがあります。

1. Finder で **アプリケーション** を開く。
2. **CapsZoom を右クリック（または Control + クリック）→「開く」**。
3. 「開発元を確認できないため開けません」と出たら、もう一度 **「開く」** を押す。
4. メニューバー右のほうに 🔍 が出れば起動成功。

2回目以降は普通にダブルクリックで開いてよいです。

---

## 3. アクセシビリティを許可する

Caps Lock の単独押しを検出するために必要です。ここが OFF だと、押しても何も起きません。

1. **システム設定** を開く（Apple メニュー → システム設定）。
2. 左の一覧から **プライバシーとセキュリティ** を開く。
3. 下のほうの **アクセシビリティ** を開く。
4. 一覧に CapsZoom があれば、右のスイッチを **ON** にする。
5. 無ければ:
   1. 一覧の下（または上）の **＋** を押す。
   2. パスワードまたは Touch ID を求められるので通す。
   3. 左側で **アプリケーション** を選び、**CapsZoom** を選んで「開く」。
   4. スイッチが ON になっていることを確認する。
6. CapsZoom が2つ並んでいる場合は、**/Applications の CapsZoom** のほうを ON にする。もう一方は OFF でよい。

---

## 4. 画面収録を許可する

拡大する画面を撮るために必要です。ここが OFF だと、押したとき真っ黒になります。

1. **システム設定** → **プライバシーとセキュリティ**。
2. **画面収録**（「画面収録とシステム音声」と書いてあることもある）を開く。
3. 一覧に CapsZoom があればスイッチを **ON**。
4. 無ければ **＋** で、アプリケーションの **CapsZoom** を追加して ON。
5. 「画面を収録するアプリに、今すぐ終了して開き直す必要があります」と出たら **「今すぐ終了して開く」** でよい。出なければ次の手順へ。

---

## 5. アプリを入れ直す

権限を付けたあと、一度 CapsZoom を終了してから起動し直してください。

1. メニューバーの 🔍 をクリック → **Quit CapsZoom**。
2. アプリケーションの CapsZoom を開く（🔍 が戻る）。

---

## 6. 使い方

- **Caps Lock 単独押し**でズーム ON / OFF。カーソル位置を中心に2倍、下の画面はライブで追従します。
- もう一度押すと元に戻ります。
- オーバーレイはクリック透過です。カーソル自体は拡大されません。
- OS の大文字ロックは使いません。本当の Caps Lock が必要なときは **Shift + Caps Lock**。

---

## 7. ログイン時に自動で起動する（任意）

1. **システム設定** → **一般** → **ログイン項目と機能拡張**（「ログイン項目」だけのこともある）。
2. 「ログイン時に開く」の **＋**。
3. アプリケーションの **CapsZoom** を追加する。

---

## うまくいかないとき

| 症状 | 確認すること |
|---|---|
| 押しても何も起きない | アクセシビリティが ON か。🔍 が出ているか。Caps Lock を単独で押したか。古い `/Applications` のコピーを開いていないか。 |
| 真っ黒になる | 画面収録が ON か。許可したあと Quit → 再起動したか。 |
| 「開発元を確認できない」 | ダブルクリックではなく、右クリック → 開く。 |
| ターミナルから起動した | やめる。アプリケーションの CapsZoom を開く。 |
| Intel Mac | 非対応（Apple Silicon のみ）。 |

権限を付け直すときは、一覧の CapsZoom を一度 **− で削除**してから、もう一度 **＋** で `/アプリケーション/CapsZoom` を足すと確実です。スイッチの入れ直しだけでは効かないことがあります。
